# Keep element-distribution analysis generic

ReacNet Scope is a general reaction molecular-dynamics evidence workbench rather than a carbon-chemistry-specific product. Element-distribution analysis therefore groups species by atom count for a user-selected element and discovers optional filter elements from the dataset; carbon may be offered as a preset when present, but C, O, or Cl must not be encoded as domain assumptions in the index schema or UI.
